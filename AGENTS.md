# AGENTS.md

## Pre-commit Instructions

Ensure that the codebase satisfies all formatters and linters. In this project:
- `black` is used for formatting.
- `flake8` is used for linting.
- `pytest` is used for testing.

Run tests to ensure everything works before submitting.
