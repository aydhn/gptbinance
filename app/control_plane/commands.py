from app.control_plane.models import CommandDefinition
from app.control_plane.enums import CommandClass, ActionClass, ScopeClass, RollbackClass

DEFAULT_COMMANDS = [
    CommandDefinition(
        command_id=CommandClass.FREEZE_SYMBOL,
        action_class=ActionClass.REVIEW_REQUIRED,
        required_scope=ScopeClass.SYMBOL,
        requires_preview=True,
        requires_approval=True,
        reversibility=RollbackClass.REVERSIBLE,
    ),
    CommandDefinition(
        command_id=CommandClass.FREEZE_SLEEVE,
        action_class=ActionClass.REVIEW_REQUIRED,
        required_scope=ScopeClass.SLEEVE,
        requires_preview=True,
        requires_approval=True,
        reversibility=RollbackClass.REVERSIBLE,
    ),
    CommandDefinition(
        command_id=CommandClass.FREEZE_STRATEGY,
        action_class=ActionClass.REVIEW_REQUIRED,
        required_scope=ScopeClass.STRATEGY,
        requires_preview=True,
        requires_approval=True,
        reversibility=RollbackClass.REVERSIBLE,
    ),
    CommandDefinition(
        command_id=CommandClass.KILL_EXECUTION,
        action_class=ActionClass.IMMEDIATE,
        required_scope=ScopeClass.ENVIRONMENT,
        requires_preview=False,  # Emergency
        requires_approval=False,
        reversibility=RollbackClass.IRREVERSIBLE,
    ),
    CommandDefinition(
        command_id=CommandClass.PAUSE_WORKFLOW,
        action_class=ActionClass.IMMEDIATE,
        required_scope=ScopeClass.GLOBAL,
        requires_preview=True,
        requires_approval=False,
        reversibility=RollbackClass.REVERSIBLE,
    ),
    CommandDefinition(
        command_id=CommandClass.REQUEST_RERUN,
        action_class=ActionClass.REVIEW_REQUIRED,
        required_scope=ScopeClass.WORKFLOW,
        requires_preview=True,
        requires_approval=True,
        reversibility=RollbackClass.IRREVERSIBLE,
    ),
    CommandDefinition(
        command_id=CommandClass.REQUEST_BACKFILL_REVIEW,
        action_class=ActionClass.REVIEW_REQUIRED,
        required_scope=ScopeClass.SYMBOL,
        requires_preview=True,
        requires_approval=True,
        reversibility=RollbackClass.REVERSIBLE,
    ),
    CommandDefinition(
        command_id=CommandClass.ROLLBACK_CANDIDATE_EXPANSION,
        action_class=ActionClass.REVIEW_REQUIRED,
        required_scope=ScopeClass.ENVIRONMENT,
        requires_preview=True,
        requires_approval=True,
        reversibility=RollbackClass.REVERSIBLE,
    ),
    CommandDefinition(
        command_id=CommandClass.UNFREEZE_SCOPE,
        action_class=ActionClass.REVIEW_REQUIRED,
        required_scope=ScopeClass.ENVIRONMENT,
        requires_preview=True,
        requires_approval=True,
        reversibility=RollbackClass.REVERSIBLE,
    ),
    CommandDefinition(
        command_id=CommandClass.INCIDENT_HOLD,
        action_class=ActionClass.IMMEDIATE,
        required_scope=ScopeClass.ENVIRONMENT,
        requires_preview=False,
        requires_approval=False,
        reversibility=RollbackClass.REVERSIBLE,
    ),
    CommandDefinition(
        command_id=CommandClass.GUARDED_RESUME,
        action_class=ActionClass.REVIEW_REQUIRED,
        required_scope=ScopeClass.ENVIRONMENT,
        requires_preview=True,
        requires_approval=True,
        reversibility=RollbackClass.IRREVERSIBLE,
    ),
]
