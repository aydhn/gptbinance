# Auto-generated integration for federation
# Rule: federated safe verdict under orphaned beneficiary draw right blocker/caution üretsin
def evaluate_performance_security_integration(context, security_records):
    cautions = []
    # Implementing: federated safe verdict under orphaned beneficiary draw right blocker/caution üretsin
    if not security_records or getattr(security_records, 'is_unfunded', False):
        cautions.append('federated safe verdict under orphaned beneficiary draw right blocker/caution üretsin')
    return cautions
