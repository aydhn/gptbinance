# Auto-generated integration for precedent
# Rule: precedent used to justify weaker security than matched pattern explicit caution üretsin
def evaluate_performance_security_integration(context, security_records):
    cautions = []
    # Implementing: precedent used to justify weaker security than matched pattern explicit caution üretsin
    if not security_records or getattr(security_records, 'is_unfunded', False):
        cautions.append('precedent used to justify weaker security than matched pattern explicit caution üretsin')
    return cautions
