# Auto-generated integration for scenario
# Rule: robust recovery claim under security-sensitive scenario gap explicit caution üretsin
def evaluate_performance_security_integration(context, security_records):
    cautions = []
    # Implementing: robust recovery claim under security-sensitive scenario gap explicit caution üretsin
    if not security_records or getattr(security_records, 'is_unfunded', False):
        cautions.append('robust recovery claim under security-sensitive scenario gap explicit caution üretsin')
    return cautions
