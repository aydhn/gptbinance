# Security Plane

The Security Plane manages assets -> boundaries -> secrets/credentials -> exposures -> patch/rotation -> trust flow.
Why vulnerability != exploitability != exposure.
Why no hidden privileged path / no unmanaged secrets.
