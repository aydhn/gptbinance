# Auto-generated integration for semantic
# Rule: security wording under semantic mismatch explicit conflict üretsin
def evaluate_performance_security_integration(context, security_records):
    cautions = []
    # Implementing: security wording under semantic mismatch explicit conflict üretsin
    if not security_records or getattr(security_records, 'is_unfunded', False):
        cautions.append('security wording under semantic mismatch explicit conflict üretsin')
    return cautions
