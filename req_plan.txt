1. Write a script `build_phase_113.py` to generate the new files for `app/jurisdiction_plane`, update existing integration files, add the required tests, and add the docs.
2. The script will create stub classes/enums/models as requested.
3. For integrations (e.g. `app/policy_plane/evaluations.py`, `app/main.py`), the script will append or insert the necessary code.
4. Run the script and fix any flake8 or pytest issues.
5. Create `app/jurisdiction_plane/__init__.py`.
6. Pre-commit check.
7. Submit.
